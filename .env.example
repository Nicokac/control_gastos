# Django
# =============================================================================
# CONFIGURACIÓN DE ENTORNO - Control de Gastos
# =============================================================================
# Copiar este archivo a .env y completar los valores
#
# IMPORTANTE: Nunca subir el archivo .env a control de versiones
# =============================================================================

# -----------------------------------------------------------------------------
# SEGURIDAD (OBLIGATORIO)
# -----------------------------------------------------------------------------

# Clave secreta - OBLIGATORIA, mínimo 50 caracteres
# Generar con: python manage.py generate_secret_key
# O con: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=

# Hosts permitidos (separados por coma, sin espacios)
# Ejemplo: tudominio.com,www.tudominio.com
ALLOWED_HOSTS=localhost,127.0.0.1

# Modo debug - NUNCA True en producción
DEBUG=True

# -----------------------------------------------------------------------------
# BASE DE DATOS
# -----------------------------------------------------------------------------

# SQLite (desarrollo) - No requiere configuración adicional
# PostgreSQL (producción) - Descomentar y configurar:

# DB_ENGINE=django.db.backends.postgresql
# DB_NAME=control_gastos
# DB_USER=postgres
# DB_PASSWORD=tu_password_seguro
# DB_HOST=localhost
# DB_PORT=5432

# -----------------------------------------------------------------------------
# EMAIL (opcional, para recuperación de contraseña)
# -----------------------------------------------------------------------------

# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_HOST_USER=tu_email@gmail.com
# EMAIL_HOST_PASSWORD=tu_app_password
# EMAIL_USE_TLS=True
# DEFAULT_FROM_EMAIL=noreply@tudominio.com
